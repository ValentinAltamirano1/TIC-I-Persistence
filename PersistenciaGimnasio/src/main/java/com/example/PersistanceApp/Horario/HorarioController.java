package com.example.PersistanceApp.Horario;

/*import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.MediaType;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping(path="api/v1/gimnasio/horario")
public class HorarioController {
    private final HorarioService horario;

    @Autowired
    public HorarioController(HorarioService horario) {
        this.horario = horario;
    }

    @GetMapping
    public List<Horario> getHorario(){

        return horario.getHorarioKey();
    }

    @PostMapping(consumes = {MediaType.APPLICATION_XML_VALUE, MediaType.APPLICATION_JSON_VALUE})
    public void registrarNuevoHorario(@RequestBody Horario horario1){
        horario.addNewHorario(horario1);
    }
}*/
